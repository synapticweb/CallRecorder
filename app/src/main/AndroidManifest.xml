<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.synapticweb.callrecorder">
    <!--android:sharedUserId="android.uid.system" -->
    <uses-feature android:name="android.hardware.telephony"
        android:required="true"/>

    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CallRecorderMainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PhoneNumberDetail"
            android:parentActivityName=".CallRecorderMainActivity">
        </activity>

        <activity android:name=".SettingsActivity"
            android:parentActivityName=".CallRecorderMainActivity">
        </activity>

        <activity android:name=".EditPhoneNumberActivity"
            android:parentActivityName=".PhoneNumberDetail">
        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>

        <activity android:name=".player.PlayerActivity"
            android:parentActivityName=".PhoneNumberDetail"
            android:configChanges="orientation|screenSize"
            android:theme="@style/PlayerStyle"/> <!--Fără screenSize continuă să se restarteze. -->

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="net.synapticweb.callrecorder.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files"/>
        </provider>


        <receiver android:name=".CallReceiver">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE"/>
                <action android:name="android.intent.action.NEW_OUTGOING_CALL"/>
            </intent-filter>

        </receiver>

        <receiver android:name=".ControlRecordingReceiver">
            <intent-filter>
                <action android:name="net.synapticweb.callrecorder.STOP_RECORDING"/>
                <action android:name="net.synapticweb.callrecorder.START_RECORDING" />
                <action android:name="net.synapticweb.callrecorder.PAUSE_RECORDING" />
                <action android:name="net.synapticweb.callrecorder.RESUME_RECORDING" />
            </intent-filter>
        </receiver>

        <service android:name=".RecorderService"/>
    </application>

</manifest>